import React from 'react';
import ReactDOM from 'react-dom';
import Login from './componentes/componentes_login/login_page';

ReactDOM.render(
  <React.StrictMode>
    <Login />
  </React.StrictMode>,
  document.getElementById('root')
);